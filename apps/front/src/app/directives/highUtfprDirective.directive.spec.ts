import { HighUtfprDirectiveDirective } from './highUtfprDirective.directive';

describe('HighUtfprDirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new HighUtfprDirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
